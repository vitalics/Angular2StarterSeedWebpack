# Angular2StarterSeedWebpack
## - write `npm install` in your root path
## - write `npm install typings -g` if don't have
## - write `npm start` and wait for building project
## - open browser in `8000` port
